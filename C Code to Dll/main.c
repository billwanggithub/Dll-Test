#include "myfunction.h"
#include <stdio.h>
#include <string.h>
main()
{
	//struct MyData myData;
	//printf("sizeof myData = %d, myData.age = %d, myData.name = %d\n", 
	//	sizeof(myData), sizeof(myData.age), sizeof(myData.name));

	//myData.age = 10;
	//strncpy_s(myData.name, sizeof(myData.name),"Bill", sizeof("Bill"));
	//printf("age = %d , name = %s\n", myData.age, myData.name);

	//struct MyData modData =  func_struct(myData);
	//printf("age = %d , name = %s\n", modData.age, modData.name);

}